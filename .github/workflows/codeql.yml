name: "CodeQL"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      matrix:
        language: [ 'javascript' ]

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@v2

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
Étape 2 : Configurer Dependabot
2.1 Créer .github/dependabot.yml
version: 2
updates:
  # Surveiller les dépendances npm
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "security"

  # Surveiller les actions GitHub
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"

  # Surveiller l'image de base Docker
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "docker"
2.2 Ajouter un package.json (pour tester)
Créer package.json à la racine :

{
  "name": "devops-tp-docker",
  "version": "1.0.0",
  "description": "TP DevSecOps avec Docker",
  "scripts": {
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "express": "4.17.1",
    "lodash": "4.17.20"
  },
  "devDependencies": {
    "eslint": "7.32.0"
  }
}
Note : Versions anciennes volontairement pour détecter des vulnérabilités.

Étape 3 : Activer Secret Scanning
3.1 Activation
Settings > Code security and analysis
Activer :
Secret scanning
Push protection (recommandé)
3.2 Test de blocage
Créer test-secret.txt :

github_token=ghp_ABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890
aws_access_key=AKIAIOSFODNN7EXAMPLE
Tenter de pusher :

git add test-secret.txt
git commit -m "Test secret detection"
git push
Le push doit être bloqué.

3.3 Nettoyage
git rm test-secret.txt
git reset --hard HEAD~1
Étape 4 : Ajouter Hadolint pour le Dockerfile
4.1 Qu'est-ce que Hadolint ?
Hadolint est un linter pour Dockerfile qui :

Vérifie les bonnes pratiques Docker
Détecte les problèmes de sécurité
Suggère des optimisations
4.2 Créer .hadolint.yaml
ignored:
  - DL3018  # Ignorer warning sur les versions pinnées Alpine
  - DL3008  # Ignorer warning sur les versions pinnées apt

trustedRegistries:
  - docker.io
  - ghcr.io

failure-threshold: warning
4.3 Tester localement
docker run --rm -i hadolint/hadolint < Dockerfile
Étape 5 : Intégrer Trivy dans le Pipeline
5.1 Qu'est-ce que Trivy ?
Trivy est un scanner de vulnérabilités pour :

Images Docker
Système de fichiers
Dépendances
5.2 Modifier le workflow docker-deploy.yml
Remplacer le contenu par :

name: Build, Scan and Push Docker Image

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  security-analysis:
    name: Security Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: ./Dockerfile
          failure-threshold: warning

  build-and-scan:
    name: Build and Security Scan
    runs-on: ubuntu-latest
    needs: security-analysis
    permissions:
      contents: read
      packages: write
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image for scanning
        uses: docker/build-push-action@v4
        with:
          context: .
          load: true
          tags: ${{ env.IMAGE_NAME }}:scan
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}:scan
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v2
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run Trivy with fail on CRITICAL/HIGH
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_NAME }}:scan
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  push-image:
    name: Push Docker Image
    runs-on: ubuntu-latest
    needs: build-and-scan
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: spdx-json
          output-file: sbom.spdx.json

      - name: Upload SBOM
        uses: actions/upload-artifact@v3
        with:
          name: sbom
          path: sbom.spdx.json
